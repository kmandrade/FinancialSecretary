{
  "name": "financial-secretary-worker",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:migrate:remote": "wrangler d1 execute financial_teste --remote --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute financial_teste --local --file=./schema.sql",
    "gen:vapid": "node ./scripts/gen-vapid-keys.js"
  },
  "dependencies": {
    "@pushforge/builder": "2.0.1",
    "fast-xml-parser": "^4.5.0",
    "jose": "^5.9.6",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260130.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.61.1"
  }
}
